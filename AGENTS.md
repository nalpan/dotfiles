# このリポジトリでの作業指示

- 指示があるまで `git push` をしないこと。

## 業務端末の情報をコミットしない

このリポジトリは **公開リポジトリ**(`github:nalpan/dotfiles`)です。以下をコミットしないこと。

- 業務端末のユーザー名
- 社内ホスト名・社内URL・社内IPアドレス
- 業務用メールアドレス

端末固有の識別子が必要になった場合は、次のいずれかで外部から与えること。

- 評価時に必要なもの(Nixの値) — `flake.nix` で実行時の環境からユーザー名を取得している仕組みを使う
- 評価時に不要なもの(シェル設定・Git設定など) — `~/.zshrc.local` / `~/.gitconfig.local` などのローカルオーバーライドに置く

## 設定ファイルの置き場所

設定ファイルを追加・変更するときは、公開ベースと端末固有の上書きを分けて置くこと。

- 公開ベース(全端末共通のデフォルト値)は `nix/files/` に置く。配置先(`$HOME` からの相対パス)をそのまま再現する
- 端末固有の上書きは `local/` に置く。`nix/files/` と同じ相対パスにする(`local/` は `.gitignore` 済みのため公開リポジトリには載らない)
- 複数の配置先で使う共有ソース(`$HOME` の特定パスに対応しないもの)は `nix/shared/` に置く
- `local/` に新しい設定を追加したら、同じディレクトリに `*.sample.json` の見本ファイルも作り、コミットすること。見本には業務固有の情報を一切書かないこと

## 新しいNixファイルを追加したら `git add` すること

`nix/` 配下(`nix/files/` / `nix/shared/` / `nix/lib/` などを含む)に新しいファイルを追加したら、ビルドを試す前に `git add` すること(コミットまでは不要)。flake はソースツリーとして **git 追跡下のファイルしか含めない**ため、ファイルがディスク上に存在していても、未追跡なら評価時には存在しないものとして扱われること。

症状は `path '.../nix/lib/merge.nix' does not exist` のようなビルドエラーで、ファイルは実際に存在するため原因に気づきにくいこと。

`local/` 配下は対象外。絶対パスで読み込むため `git add` は不要で、`.gitignore` 済みのためそもそも追加しないこと。

## READMEの「管理対象」を最新に保つ

以下を変更したら、**同じコミット内で** `README.md` の「管理対象」セクションも更新すること。追加・削除・リネームのいずれも対象。

| 変更した箇所 | 更新するREADMEの項目 |
|---|---|
| `nix/home.nix` の `home.packages` | 「CLIパッケージ」 |
| `nix/home.nix` の `home.file` / `programs.*` | 「配置ファイル」の表 |
| `nix/files/` / `local/` のファイル追加 | 「配置ファイル」の表 |
| `nix/darwin.nix` の `homebrew.casks` | 「GUIアプリ・フォント」 |

READMEの記載はNixファイル上の識別子(パッケージ名・cask名・属性名)をそのまま使い、目視で突き合わせられる状態を保つこと。
